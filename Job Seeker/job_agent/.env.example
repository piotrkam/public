# The Agent SDK uses the Claude Code CLI, which authenticates via your
# Claude subscription — no API key needed.
#
# Setup (one time):
#   npm install -g @anthropic-ai/claude-code
#   claude login
#
# That's it. No .env file required unless you want to override something.
